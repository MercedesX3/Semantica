"use client";

/**
 * Book Scroll — same layout/chrome as Browse until a dedicated scroll feed ships.
 * Keeping this as its own route so the nav can highlight correctly.
 */
export { default } from "../home/page";
