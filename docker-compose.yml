version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: storydna-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: storydna
    ports:
      - "5432:5432"
