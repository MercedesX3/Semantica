# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data handling
pydantic==2.8.2
numpy==1.26.4
pandas==2.2.2

# Database
sqlalchemy==2.0.32
psycopg2-binary==2.9.9
pgvector==0.3.6

# Authentication
# app/core/security.py uses pwdlib (Argon2id) + PyJWT. passlib and python-jose
# were pinned here but never imported, while pwdlib — which the code actually
# requires — was missing, so a clean install failed at import.
pwdlib==0.3.0
argon2-cffi==25.1.0
PyJWT==2.13.0
email-validator
boto3

# File uploads (PDF ingestion)
pypdf==6.15.0
python-multipart==0.0.32

# NLP + ML
torch
transformers==4.44.2
sentence-transformers==3.0.1
scikit-learn==1.5.1

# Text processing
nltk==3.9.1
spacy==3.7.5

# Utilities
python-dotenv==1.0.1
tqdm==4.66.5
requests==2.34.2

# Optional (but useful for experimentation)
matplotlib==3.9.2
seaborn==0.13.2